[package]
name = "Rex"
version = "1.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
tar = "0.4"
zstd = "0.13"
fs_extra = "1.3"

[profile.release]
opt-level = "z"
panic = "abort"
codegen-units = 1
lto = true
strip = true
debug = false
overflow-checks = false